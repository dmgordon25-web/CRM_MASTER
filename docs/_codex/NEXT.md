# NEXT
Run: npx playwright test tests/e2e/action_bar_selection.spec.js --project=chromium --workers=1 --retries=0
Then continue full gate from `npm run test:counts` onward after verifying webserver stability.
