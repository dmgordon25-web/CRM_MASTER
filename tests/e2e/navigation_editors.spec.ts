import { test, expect } from '@playwright/test';

// Placeholder smoke to keep navigation editor workflow runnable in CI.
// Full navigation coverage lives in broader Playwright suites.
test('navigation editors smoke', async () => {
  expect(true).toBe(true);
});
